import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server:{
    headers: {
      // Assurez-vous que chaque directive est correctement définie
      'Content-Security-Policy': [
        "default-src 'self'",
        // Ajout de style-src-elem spécifiquement pour Google Fonts
        "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
        "style-src-elem 'self' 'unsafe-inline' https://fonts.googleapis.com",
        "font-src 'self' https://fonts.gstatic.com",
        // Pour votre API
        "connect-src 'self' https://api-aenat.alljacquinot.tech",
        // Pour le développement Vite
        "script-src 'self' 'unsafe-inline' 'unsafe-eval'",
        "img-src 'self' data: blob: https:",
      ].join('; ')
    },
  },

  // Ajout de la configuration de preview pour la production
  preview: {
    headers: {
      'Content-Security-Policy': [
        "default-src 'self'",
        "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
        "style-src-elem 'self' 'unsafe-inline' https://fonts.googleapis.com",
        "font-src 'self' https://fonts.gstatic.com",
        "connect-src 'self' https://api-aenat.alljacquinot.tech",
        "img-src 'self' data: blob: https:",
      ].join('; ')
    }
  }

  define:{
    'process.env': {},
  },
  base: '/',
})
